describe("SwapTokenForm", () => {

  it("passes", () => {
    cy.visit("http://localhost:3000/en");
  });
});

describe("check amount", () => {
  // it("check amount", () => {
  //   cy.get('#input-amount').type("2000"); // Assuming the input field has a type of "Number"
  // });
});