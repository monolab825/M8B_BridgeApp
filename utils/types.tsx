export type chainType = {
  chainType: number,
  chainIcon: any,
  chainName: string,
  btnText: string,
  tokenIcon: any,
  tokenText: string,
  tokenAmount: number,
  tokenBalance: number,
  tokenPrice: number,
  accountInfo: {
    address: string,
    balance: number
  },
}
